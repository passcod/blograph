kind: Service
apiVersion: v1
metadata:
  labels:
    app: blograph
  name: blograph
  namespace: default
spec:
  type: NodePort
  selector:
    app: blograph
  ports:
  - name: blograph
    protocol: TCP
    port: 80
    targetPort: 5000
